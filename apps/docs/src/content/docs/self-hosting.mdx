---
title: Self-hosting
---

## Quickstart (Docker Compose)
From repo root:

```bash
docker compose -f docker/compose.yml up
```

Then open:

* Web: http://localhost:3000

## Notes

* This uses local Postgres by default.
* Telemetry is disabled by default unless enabled via env.
